

const ParentDashboard = ({ user }) => (
  <div>
    <h1>Parent Dashboard</h1>
    <p>Welcome, {user.displayName}!</p>
    {/* Add parent functionality here */}
  </div>
);

export default ParentDashboard;